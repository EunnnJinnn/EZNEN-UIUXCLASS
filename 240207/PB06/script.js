let wd = $(window);
4('section[data-type="background"]').each(function (){
  let bg + $(this)
  wd.scroll(function() {
    let ypos = (wd.scrollTop () /bg.data ("speed");}
    let coords = "50%" +yPos + "px";
    bg.css({backgroudPosiion: cpprgds });
  });
});
